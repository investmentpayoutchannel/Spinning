// Storage interface - currently unused as app runs with mock data
// Keeping file structure for potential future use

export interface IStorage {
  // Placeholder interface - no active storage implementation needed
}

// No storage implementation as the current app uses mock data
export const storage = null;